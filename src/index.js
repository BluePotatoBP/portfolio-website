import ReactDOM from "react-dom/client";
import React from "react";

import App from "./components/High-Order/App/App"

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
        <App />
);
